[build]
  # ビルドイメージをUbuntu 22.04 (Jammy)に指定
  image = "ubuntu-22.04"
  # ビルドコマンドを指定
  command = "npm run build"

[build.environment]
  # Node.jsバージョンを16に固定（undiciエラー回避のため）
  NODE_VERSION = "16"

# Gatsbyプラグインを一時的に無効化（undiciエラー回避のため）
# [[plugins]]
#   package = "@netlify/plugin-gatsby"
